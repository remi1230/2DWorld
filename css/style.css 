* {
    box-sizing: border-box;
}

body {
    margin: 0;
    padding: 20px;
    background: linear-gradient(135deg, #0a0a1a 0%, #1a1a2e 100%);
    color: #e0e0e0;
    font-family: 'Segoe UI', 'Roboto', sans-serif;
    min-height: 100vh;
}

h1 {
    text-align: center;
    color: #4af;
    margin-bottom: 10px;
    font-weight: 300;
    letter-spacing: 2px;
}

.subtitle {
    text-align: center;
    color: #666;
    margin-bottom: 20px;
    font-style: italic;
}

/* ===== LAYOUT PRINCIPAL ===== */

.game-container {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 20px;
    max-width: 1200px;
    margin: 0 auto;
}

@media (max-width: 900px) {
    .game-container {
        grid-template-columns: 1fr;
    }
}

/* ===== CANVAS ===== */

.canvas-wrapper {
    position: relative;
}

canvas {
    display: block;
    width: 100%;
    height: auto;
    border: 2px solid #333;
    border-radius: 8px;
    background: #000;
}

.message {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 20px 40px;
    border-radius: 10px;
    font-size: 24px;
    font-weight: bold;
    text-align: center;
    display: none;
    animation: fadeIn 0.3s ease;
}

.message.success {
    background: rgba(40, 180, 100, 0.9);
    color: white;
    box-shadow: 0 0 30px rgba(40, 180, 100, 0.5);
}

.message.failed {
    background: rgba(180, 60, 60, 0.9);
    color: white;
    box-shadow: 0 0 30px rgba(180, 60, 60, 0.5);
}

@keyframes fadeIn {
    from { opacity: 0; transform: translate(-50%, -50%) scale(0.8); }
    to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
}

/* ===== PANNEAU DE CONTRÔLE ===== */

.controls {
    background: rgba(30, 30, 50, 0.8);
    border-radius: 12px;
    padding: 20px;
    border: 1px solid #333;
}

.panel {
    margin-bottom: 25px;
}

.panel:last-child {
    margin-bottom: 0;
}

.panel h3 {
    color: #4af;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin: 0 0 15px 0;
    padding-bottom: 8px;
    border-bottom: 1px solid #333;
}

/* ===== NIVEAU INFO ===== */

#levelInfo {
    background: rgba(0, 0, 0, 0.3);
    padding: 12px;
    border-radius: 6px;
    margin-bottom: 15px;
    line-height: 1.5;
}

.stats {
    display: flex;
    justify-content: space-between;
    font-size: 14px;
    color: #888;
}

/* ===== CONTRÔLES ===== */

.control-group {
    margin: 12px 0;
    display: flex;
    align-items: center;
    gap: 10px;
}

.control-group label {
    flex: 0 0 80px;
    font-size: 13px;
    color: #aaa;
}

.control-group input[type="range"] {
    flex: 1;
    height: 6px;
    -webkit-appearance: none;
    background: #333;
    border-radius: 3px;
    outline: none;
}

.control-group input[type="range"]::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: #4af;
    cursor: pointer;
    transition: background 0.2s;
}

.control-group input[type="range"]::-webkit-slider-thumb:hover {
    background: #5bf;
}

.value {
    flex: 0 0 50px;
    text-align: right;
    font-family: 'Courier New', monospace;
    color: #4af;
    font-size: 13px;
}

/* ===== BOUTONS ===== */

.buttons {
    display: flex;
    gap: 10px;
    margin-top: 15px;
}

button {
    flex: 1;
    padding: 12px 20px;
    border: none;
    border-radius: 6px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    text-transform: uppercase;
    letter-spacing: 1px;
}

button:disabled {
    opacity: 0.4;
    cursor: not-allowed;
}

.btn-primary {
    background: linear-gradient(135deg, #4af, #28a);
    color: white;
}

.btn-primary:hover:not(:disabled) {
    background: linear-gradient(135deg, #5bf, #39b);
    transform: translateY(-1px);
    box-shadow: 0 4px 15px rgba(68, 170, 255, 0.3);
}

.btn-secondary {
    background: #333;
    color: #aaa;
}

.btn-secondary:hover:not(:disabled) {
    background: #444;
    color: #fff;
}

.btn-success {
    background: linear-gradient(135deg, #4a5, #283);
    color: white;
}

.btn-success:hover:not(:disabled) {
    background: linear-gradient(135deg, #5b6, #394);
}

/* ===== SELECT ===== */

select {
    width: 100%;
    padding: 10px;
    background: #222;
    border: 1px solid #444;
    border-radius: 6px;
    color: #eee;
    font-size: 14px;
    cursor: pointer;
}

select:focus {
    outline: none;
    border-color: #4af;
}

/* ===== SECTION CAMÉRA ===== */

.camera-controls {
    opacity: 0.7;
    transition: opacity 0.3s;
}

.camera-controls:hover {
    opacity: 1;
}

.camera-controls .control-group label {
    flex: 0 0 70px;
}

/* ===== AIDE ===== */

.help {
    background: rgba(0, 0, 0, 0.3);
    padding: 15px;
    border-radius: 8px;
    font-size: 12px;
    line-height: 1.6;
    color: #888;
}

.help strong {
    color: #4af;
}

.help ul {
    margin: 10px 0 0 0;
    padding-left: 20px;
}

.help li {
    margin: 5px 0;
}

/* ===== INDICATEURS ===== */

.indicator {
    display: inline-block;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    margin-right: 8px;
}

.indicator.start {
    background: #4a5;
}

.indicator.goal {
    background: #4af;
}

.indicator.mass {
    background: #fa4;
}